const navItems=[["overview","Overview"],["business","Business"],["financials","Financials"],["analysis","Analysis"],["capital","Capital Allocation"],["valuation","Valuation"],["research","Research"],["audit","Audit"]];
export function layout({state,companies,content}){const options=Object.entries(companies).map(([ticker,c])=>`<option value="${ticker}" ${ticker===state.ticker?"selected":""}>${ticker} — ${c.name}</option>`).join("");const nav=navItems.map(([id,label])=>`<button data-page="${id}" class="${state.page===id?"active":""}">${label}</button>`).join("");return `<div class="shell"><aside class="sidebar"><div class="brand">Equity Research Terminal</div><div class="tagline">Every number traceable.<br>Every valuation explainable.</div><nav class="nav">${nav}</nav></aside><main class="main"><header class="topbar"><select id="tickerSelect" class="ticker-select">${options}</select><input class="search" value="${state.ticker}" aria-label="Company search" placeholder="Search ticker or company"><div class="top-meta">Milestone 1 · Foundation</div></header><section class="content">${content}</section></main></div>`}
