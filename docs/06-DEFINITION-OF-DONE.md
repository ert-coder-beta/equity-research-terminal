# Definition of Done

A feature is done only when it works on desktop and mobile, preserves existing modules, handles empty and error states, shows data classification, exposes formula/source lineage where relevant, has a clear purpose and updates the documentation.
